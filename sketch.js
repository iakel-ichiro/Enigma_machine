
function setup() {
    // Sets the screen to be 720 pixels wide and 400 pixels high
    createCanvas(720, 800);
    background(255);
    user_input();
}

function draw() {
    background(255);
    render();
}
